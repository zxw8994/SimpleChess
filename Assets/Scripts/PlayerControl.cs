using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerControl : MonoBehaviour {

    /*public float raycastDist;
    RaycastHit whatsHit;

    private bool player1sTurn = false;

    private ChessTile selectedTile;
    private ChessTile moveToTile;
    private int moveToCounter = 0;

    private bool selectedAPiece = false;
    private ChessPiece selectedPiece;

    private void Start()
    {
        Chess chess = GameObject.Find("Game Manager").GetComponent<Chess>();
    }

    private void Update()
    {
        Debug.DrawRay(this.transform.position, this.transform.forward * raycastDist, Color.magenta);

        if (Physics.Raycast(this.transform.position, this.transform.forward, out whatsHit ,raycastDist, 6))
        {

        }

    }

    */

}
